<div class="meat-page">
  <h1>Meats</h1>

  <% if @meat_products.any? %>
    <% @meat_products.each do |meat_product| %>
      <div class="meat-item">
        <p><%= meat_product.name %></p>
        <%= form_with url: add_product_cart_path, method: :post, local: true, html: { id: "add-to-cart-form-#{meat_product.id}" } do |form| %>
          <%= form.hidden_field :product_id, value: meat_product.id %>
          <%= form.number_field :quantity, min: 1, value: 1 %>
          <%= form.submit "Add to Cart", class: 'btn' %> <!-- Updated class here -->
        <% end %>
      </div>
    <% end %>
  <% else %>
    <p>No meat products available.</p>
  <% end %>

  <div class="navigation-buttons">
    <%= link_to 'Back', root_path, class: 'btn btn-secondary' %> <!-- Back button -->
    <%= link_to 'See Cart', cart_path, class: 'btn btn-primary' %> <!-- See Cart button -->
  </div>
</div>
