<ul id="lists">
  <% @lists.each do |list| %>
    <li>
      <%= link_to list.name, list_path(list) %>
      <ul>
        <% list.items.each do |item| %>
          <li><%= item.name %> - <%= item.category.name %></li>
        <% end %>
      </ul>
    </li>
  <% end %>
</ul>
