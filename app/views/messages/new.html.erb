<%= turbo_frame_tag "messages" do %>
  <%= form_with(model: @message, url: conversation_messages_path(@conversation), data: { turbo: true }) do |form| %>
    <%= form.text_area :content %>
    <%= form.submit 'Send' %>
  <% end %>
<% end %>
